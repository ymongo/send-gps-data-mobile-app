// This is a Capacitor (client-only) app. Disable server-side rendering so the
// page is rendered entirely in the WebView — this avoids the SSR + hydration
// delay where event handlers aren't attached on the very first interaction.
export const ssr = false;
export const prerender = true;
